# 手柄 / 摇杆 / 键位 诊断报告

日期：2026-09-15 · 产物 `Player-BwKb4RpM.js` md5 `25a595d14b4d2a5ce1f0f524eb83d184`（154662 B）
验证方式：**本机真实 Google Chrome**（远程调试 + CDP 直连），不再是逻辑回放
> 变更记录：本报告随修复持续更新。第十一章是**最新一次修复**（面板拨摇杆绑定 + 改完键被重置），
> 第三章的最初结论（「不该有反应」）已被该修复**取代**，保留原文仅为记录当时的判断依据。

---

## 一、结论摘要

| 你的问题 | 结论 |
|---|---|
| 「之前摇杆可以，现在怎么就不行了」 | **摇杆链路完好，一行都没坏。** 四条实测证据见第二节 |
| 「改键面板里只识别方向按键、不识别摇杆」 | **设计使然，不是 bug** —— 面板绑的是「按钮号」，摇杆是「轴」，两者是两套东西（第三节） |
| 「手柄键位不对 / 改 ABXY 没效果」 | 🔴 **找到真根因并已修复上线**：站点默认键位**从来就没下发到模拟器核心**（第四节，本次最大发现） |
| 「改完键想立刻生效」 | ✅ **已实现**：按键面板底部新增「应用并重启」按钮（第八节） |
| 去重把按钮 `0` 当空值 | 已于 09-14 夜修复上线（第五节） |

---

## 二、摇杆：四条实测证据

1. **不是部署/缓存不一致**：本地 / 容器内 / HTTP 分发三处 md5 完全一致。
2. **平台判定没问题**：线上 `roms.platform` 只有 `arcade`×13、`nes`×1、`megadrive`×1，**全部命中摇杆白名单**。
3. **配置确实写进了核心配置**：真实 Chrome 里抓到运行时日志
   ```
   [patch] analog-dpad {"platform":"nes","cfg":{"input_player1_analog_dpad_mode":3,
                                               "input_player2_analog_dpad_mode":3,
                                               "input_player3_analog_dpad_mode":3,
                                               "input_player4_analog_dpad_mode":3}}
   ```
   —— **四个端口全写 3**，所以「只有 1P 有摇杆」这类猜想不成立。
4. **没动过摇杆代码**：与改动前基线逐项对比，`analog_dpad_mode` / `__arcadeAnalogDpadCfg` /
   `__arcadeIsDigitalOnly` / `arcade-analog-dpad` 出现次数完全相同。

---

## 三、当时为什么判定「面板推摇杆没反应」是设计使然（⚠️ 结论已被第十一章取代）

> **后记（2026-09-15，见第十一章）**：当时这段分析在「代码事实」层面没错 —— 面板确实只扫 `buttons`。
> 但我把它当成了**设计**，而用户的诉求是**能绑**。经用户两次澄清（「在上的位置拨动左摇杆没有在对应键位显示内容」）
> 后已按 bug 处理并修复：面板新增扫 `axes` 的绑定分支 + 下发 `input_playerN_<键>_axis`。
> **下面保留的是当时的推理，不是最终结论。**

面板的绑定循环只扫 `buttons`，**完全不读 `axes`**：

```js
const M = (...navigator.getGamepads()...).filter(Boolean)[0];
for (let P = 0; P < M.buttons.length; P++) { /* 只看 buttons，没有 M.axes */ }
```

真实 Chrome 上的对照实验（决定性）：

| 操作 | 结果 |
|---|---|
| 让某一行进入绑定态后，**推左摇杆轴**（`axes[1] = -1`）持续 2.5 秒 | 仍显示「按一个键…」，**无反应** |
| 同一个绑定态，**按手柄按钮 5** | **立刻**变成 `RB` ✅ |

它等的是**按钮号**（要写进 `input_playerN_<键>_btn=按钮号`）；摇杆属于**轴**，是配置层的另一套东西。
「摇杆当方向键」由**核心级**开关 `analog_dpad_mode=3` 负责，对 nes / famicom / gb / gba / arcade
这些纯数字主机已经自动开着。
⇒ **打游戏时推摇杆能走；在设置面板里推摇杆不该有反应。这是正确行为。**

---

## 四、🔴 真根因：站点默认键位从未下发核心（已修复）

### 怎么发现的

用时间线探针在真实 Chrome 上盯住启动过程，结果非常干净：

```
t=39ms   SET __arcadeGamepad  {a:1,b:0,x:3,y:2,...}   ← 出厂硬编码
t=48ms   SET __arcadeGamepad  同上
t=304ms  effGm() = {a:1,b:0,x:3,y:2,...}  |  kp=undefined   ← 平台一直没被设置
...一路到 10 秒后，仍然是出厂值，从未变过
```

`defaultsLoaded = ["nes","arcade"]` —— 站点默认**接口明明返回了** `nes.gamepad = {a:0,b:2,x:1,y:3}`。

### 直接看核心拿到了什么

从 Nostalgist 实例上读**真正交给模拟器核心的 `retroarchConfig`**：

| 配置项 | 修复前（实际下发） | 站点 NES 默认（应下发） |
|---|---|---|
| `input_player1_a_btn` | **1** | **0** |
| `input_player1_b_btn` | **0** | **2** |
| `input_player1_x_btn` | **3** | **1** |
| `input_player1_y_btn` | **2** | **3** |

**全是出厂硬编码。** 2P 也一样。

### 代码上的原因

- `effGm()` 用 `platform()` 去取站点默认表；`platform()` 在 `__arcadeKeyPlatform` 未设置时返回 `'__global__'`；
- 而站点数据库里**只有 `nes` 和 `arcade` 两行默认，没有 `__global__` 行** → 取不到 → 直接退化成出厂 `ht`；
- `__arcadeKeyPlatform` 偏偏**只在打开「按键设置」面板时**才被设置（面板组件 `setup()` 里那一句调用）；
- 模块尾那句「启动时预拉一次站点默认」又要求 `__arcadeKeyPlatform` 已存在 —— 启动时它必然不存在，**这个分支永远不会执行**。

于是「玩家进游戏、不打开按键面板」这条**主路径**上，站点默认完全没生效。
而玩家在面板里改键时，因为面板会先设平台、`__arcadeGamepad` 变对，但**核心已经启动完毕、改键不会重启核心**；
退出重进后 `__arcadeGamepad` 又退回出厂值 —— 玩家改的 diff 被叠加在**错误的基准**上，
**所以「怎么改都不对」**。

### 修复

新增 `[patch] key-platform-sync`（脚本 `_patch_keysync.py`，已上线）：

1. 在模拟器启动函数里、**真正装配核心配置之前**，先
   `setKeyPlatform(平台)` → `await 拉取站点默认` → `await 合并并应用`；
2. `__arcadeSyncPlatformKeys` 的「未登录 / 接口不可用」两个提前返回分支，
   改为**至少把站点默认应用上**（原来直接 `return null`，等于键位永远停在出厂值）；
3. 启动预拉去掉那个永不成立的前置条件。

### 修复后复验（真实 Chrome，10/10 断言通过）

```
t=66ms  SET keyPlatform "nes"                       ← 平台在启动阶段就位
t=68ms  SET __arcadeGamepad {a:0,b:2,x:1,y:3,...}   ← 站点默认生效
```

核心配置：`input_player1_a_btn=0 / b_btn=2 / x_btn=1 / y_btn=3`，1P 与 2P 全部正确 ✅
（用真实注册账号复验：10/10 断言通过）

**另外还补了同类的一个分支**：如果站点接口取不到你的键位（会话过期、401、网络抖动），
原来也会悄悄退回出厂键位 —— 现已改为**至少用站点默认兜底**。所以以后不会再有「键位莫名其妙变回默认」。

---

## 五、已修复：按钮号 `0` 被当成「没绑定」（09-14 夜）

去重函数第二轮原为 `if (!v) continue;` —— 按钮号 `0`（标准布局的 A 键）是 falsy，被整个跳过，
导致绑在 0 号按钮上的键**永远不参与去重**。用你的真实数据回放，后果是：

- 你自定义的 `left = 按钮 0`，平台默认的 `a = 按钮 0`
- 两个都下发 → `input_player1_left_btn=0` 与 `input_player1_a_btn=0` 同时存在
- 核心两个都认 → **按手柄 A 键会连带触发「左」**，而面板上完全看不出来

**修复**：判空只用 `v === void 0 || v === null || v === ''`，不用 falsy。
代价：冲突无法两全时，`a` 会变成 `'nul'`（面板显示为未绑定）—— 比悄悄双触发要诚实。
测试 `test_key_platform.js` **69 PASS / 0 FAIL**。

---

## 六、你的键位数据里还有一处冲突（需要你决定）

| 位置 | gamepad 内容 |
|---|---|
| `input_defaults.nes` / `.arcade`（站点默认） | `{a:0, b:2, x:1, y:3, l:4, r:5, l2:6, r2:7, select:8, start:9, up:12, down:13, left:14, right:15}` |
| `input_mappings[你, __global__]` | `{b:3, down:1, left:0}` ← **你自己改的** |
| `input_mappings[你, nes]` | `{a:0, b:2, x:1, y:3}` ← 与站点默认逐字相同，是旧版整份快照遗留的脏数据，可安全删除 |
| `input_mappings[你, arcade]` | `{a:3, y:2}` |

因为 `down=按钮1`、`left=按钮0` 是你自己改的（出厂是 13 / 14），最终结果是：

| 平台 | up | down | left | right | a | b | x | y | l | r | select | start |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| nes | 12 | **1** | **0** | 15 | — | 2 | — | 3 | 4 | 5 | 8 | 9 |
| arcade | 12 | **1** | **0** | 15 | 3 | — | — | 2 | 4 | 5 | 8 | 9 |

（`—` = 未绑定，按了没反应）

**如果你的手柄十字键是标准的 12–15**，那现象就是：
**按十字键「下」「左」没反应，而按 A / B 键反而变成「左」「下」。**

> ✅ 你选择了「**先别动，我自己进面板改**」，所以你的键位数据我一行都没改。
> 想理顺的话：进「按键设置 → 手柄」，把 **上 / 下 / 左 / 右** 重绑回 **12 / 13 / 14 / 15** 即可
> （绑完点新加的「应用并重启」按钮就能立刻生效）。

---

## 七、「应用并重启」按钮（已实现，你要的「改完键立刻生效」）

**为什么要重载**：模拟器的按键映射是**核心启动时**从 `retroarch.cfg` 读进去的，而模拟器只在
ROM / 核心变化时才重启 —— 所以光改键、不重启核心，核心手里的还是旧映射。这不是配置没保存，
是**核心不会自己重读**。

**做法**：在「按键设置」面板底部加了一个「**应用并重启**」按钮（在「重置手柄」和「完成」之间）。
点它会：① 自动存档 → ② 重载模拟器（重新装配配置，读到新键位）→ ③ 自动读档。
**如果拿不到存档状态，它就不重载**（宁可让你退出重进，也不冒丢进度的风险）。

真实 Chrome 上的实测（4 项全部通过）：

| 步骤 | 结果 |
|---|---|
| 把手柄「上」绑到按钮 7（RB） | 面板显示 `上 RT`，`__arcadeGamepad.up = 7` |
| 点「应用并重启」 | 按钮显示 `已应用 ✓`（含重载 + 读档成功） |
| 新核心配置 `input_player1_up_btn` | **7** ✅ 改键立刻生效 |
| 站点默认 `input_player1_a_btn` | 仍为 0 ✅（修复没被破坏），游戏画面仍在 |

> 不想用它也行：改完键**退出游戏再进**同样生效。
> 想隐藏这个按钮：控制台执行 `localStorage['arcade-keys-live']='0'`。

---

## 八、仍然建议你做一次 60 秒真机自检

走**局域网 IP**（`http://192.168.1.100:3000`），别走 CF 域名（`.js` 被 CF 缓存 4 小时）。
进游戏后按 **F12**，控制台里应能看到：

```
[patch] key-platform-sync {"platform":"nes","gamepad":{"a":0,"b":2,"x":1,"y":3,...}}
[patch] analog-dpad {"platform":"nes","cfg":{"input_player1_analog_dpad_mode":3,...}}
```

再粘贴这段，把手柄的轴值发我：

```js
var g=[...((globalThis.__arcadeRealPads?__arcadeRealPads():navigator.getGamepads())||[])].filter(Boolean);
copy(JSON.stringify({
  开关: localStorage.getItem('arcade-analog-dpad'),
  路由: localStorage.getItem('arcade-router'),
  设备: g.map(p=>({id:p.id, mapping:p.mapping, axes:[...p.axes].map(n=>+n.toFixed(2))}))
}, null, 1))
```

- `platform` 是 `nes` 且 `gamepad` 是 `a:0,b:2,x:1,y:3` → 站点默认已正确下发
- 推摇杆时 `axes` 里有一对非零数，但游戏里不走 → 是核心/浏览器层，我再往下查

---

## 九、如实说明：还剩什么没验

- **手柄实体按键的手感、摇杆实际走位、联机双人**，最终仍需你在真机上按一下 —— 这些无法远程替代。
- 程序层面能验证的（站点默认是否下发 / 面板是否识别按钮 / 平台是否在启动前就位 / 改键后核心是否真的换上新配置）
  本次都在**真实 Chrome** 上验完了。
- 本次顺手做的清理：删除了我诊断时注册的一次性测试账号，容器里的临时脚本也清了；
  **你自己账号的 3 行键位数据一行没动**（按你的要求）。

---

## 十、双截龙格斗（街机）按键 + 摇杆实测记录

按你的要求，用**双截龙格斗**（rom id=18，平台 `arcade`，核心 **FinalBurn Neo**）
在真实 Chrome 上做了一轮完整的按键 + 摇杆端到端测试。

### 1. 核心真正拿到了什么（14/14 全部一致）

把「玩家有效手柄键位」与「核心配置里的按钮号」逐项对账：

| 逻辑键 | 玩家有效键位 `__arcadeGamepad` | 核心 `input_player1_*_btn` | 一致 |
|---|---|---|---|
| up / down / left / right | 12 / 13 / 14 / 15 | 12 / 13 / 14 / 15 | ✅ |
| a / b / x / y | 0 / 11 / 1 / 3 | 0 / 11 / 1 / 3 | ✅ |
| l / r / l2 / r2 | 4 / 5 / 6 / 7 | 4 / 5 / 6 / 7 | ✅ |
| select / start | 8 / 9 | 8 / 9 | ✅ |

**14 个键参与比对，0 个不一致。** 其中 `b = 11` 是我在面板里改的（见下面的说明），
改完点「应用并重启」后，**新核心实例确实拿到了 11** —— 保存 → 下发 → 重启整条链路通了。

其它项：1P `joypad_index=0`、2P `joypad_index=1`；
`input_player1_analog_dpad_mode=3`、`input_player2_analog_dpad_mode=3`。

### 2. 摇杆平台白名单（对 15 个平台实测）

| 平台 | 是否下发摇杆当方向键 |
|---|---|
| `arcade` / `neogeo` / `nes` / `snes` | ✅ 下发 `=3` |
| `cps1` / `cps2` / `mame` | ❌ 不下发（本来就不在白名单） |
| `psx` / `n64` / `psp` / `dreamcast` / `saturn` / `nds` | ❌ 拒发（这些主机摇杆有别的用途） |
| 空平台 / 未知平台 | ❌ 拒发（宁可不生效，也不乱发） |

> 小提示：街机游戏如果平台选的是 `cps1`/`cps2`/`mame` 而不是 `arcade`，摇杆当方向键会**不生效**。
> 建议街机 ROM 统一用 `arcade` 平台（双截龙格斗就是 `arcade`，一次通过）。

### 3. 面板对「摇杆轴」和「手柄按钮」的反应（对照实验）

- 进入绑定态后**推左摇杆 2.5 秒** → 面板仍显示「按一个键…」，**不会误绑定**（符合设计：面板绑的是按钮号，摇杆是轴）
- 同一行**按手柄按钮 11** → 立刻绑定并显示 `R3` ✅

### 4. 真实输入链路（三张截图为证）

| 截图 | 画面证据 |
|---|---|
| `_dd_1_demo.png` | `INSERT COIN` / `CREDIT 00`，底部 HUD：`1P 键盘 · 2P 手柄1` |
| `_dd_2_after_start.png` | `CREDIT 00 → 03`（投币＝按钮8 按两次）→ 出现 `PUSH START`（按钮9）→ HUD 翻转为 `1P 手柄1 · 2P 键盘`（**按 START 入座成功**） |
| `_dd_3_after_stick.png` | 推左摇杆轴后，画面提示切到 `MOVE JOYSTICK UP ONCE TO JUMP`，**1P 角色做出动作** → 摇杆当方向键真的驱动了街机角色 |

### 5. 一次误报，如实说明

测试中一度判定「改键没生效」：我把面板里的 **A** 绑到按钮 11，重启后却发现 `input_player1_a_btn` 仍是 0。
追下去发现是**我测错了键**，不是功能坏了：

> 街机平台的面板做了**语义重映射**。上游 `platforms` 里写着
> `arcade: { a:"B", b:"A", x:"D", y:"C", l:"E", r:"F" }` —— 因为在 libretro 里
> **FBNeo 的第一个按键是 `b`**，所以面板显示「A」时，绑的其实是内部 `b`。
> 我改的是 `b`（改完 `b_btn=11` ✅），却一直在断言 `a`（自然是 0）。
>
> 所以你在按键面板里看到的是 **上 / 下 / 左 / 右 / A / B / C / D / E / F**，
> 这就是街机按钮的叫法，不需要你关心背后叫什么名字。

### 6. 清理

删除了这轮注册的一次性测试账号（连带它的键位记录和登录会话）。
**你自己账号的 3 行键位数据仍然一行没动。** 本轮**没有改动任何线上代码**（纯验证）。

---

## 十一、✅ 已修复：面板能拨摇杆绑上/下/左/右（09-15，回应你的原话）

> 你的原话：「手柄按键设置，用摇杆向上拨作为上无效」
> 澄清后：「我在设置手柄的按键映射时，**在上的位置拨动左摇杆没有在对应键位显示内容**」
>
> **是我上一轮判错了 —— 这不是设计，是缺口。已修好上线。**

### 1. 缺口在哪

面板的绑定循环只遍历 `M.buttons`，**从不读 `M.axes`**。所以拨摇杆时：
没有任何分支去消费它 → 绑定态一直挂着 → 槽位永远显示「按一个键…」。

同时，无论面板还是核心配置，那时**只会写 `input_playerN_<键>_btn=按钮号`**，
没有任何地方会写 `input_playerN_<键>_axis=<±轴号>` —— 就算手动把值塞进去也没人下发。

### 2. 改了什么（`[patch] axis-bind`）

| 改动点 | 内容 |
|---|---|
| 绑定循环新增扫轴分支 | 轴值 `≥0.6` 记 `+轴号`、`≤-0.6` 记 `-轴号`；同一方向要先回中再推第二次才能重绑（防抖） |
| 新增 token 约定 | 摇杆绑定值存成字符串 **`"<±轴号>"`**（`-1`=左摇杆↑、`+1`=左摇杆↓、`-0`=左摇杆←、`+0`=左摇杆→、`±2/±3`=右摇杆），与**数字**按钮号天然区分 |
| 面板显示轴名 | 槽位从「按钮 12」变成 **「左摇杆↑」** 这类人话（`__arcadeAxisName`） |
| 下发改走轴 | `__arcadeSlots()` 遇到 `"±N"` 写 `input_playerN_<键>_axis`、**不再**写 `_btn`（避免按钮+轴双重触发） |
| token 白名单 | `__arcadeAxisTok` 只认 `/^[+-][0-3]$/`；`'nul'`、`'1'`、数字按钮号都**不会**被误判成轴 |
| 去重兼容 | 去重比较支持字符串值，防止轴 token 被当成「空」而漏过冲突检查 |
| 提示文案 | 「按一个键…」→ **「按按键或拨摇杆…」** |

轴号约定（RetroArch 标准）：左摇杆 X=0 / Y=1，右摇杆 X=2 / Y=3；`+` 正方向、`-` 负方向。
所以「左摇杆向上」就是 `input_player1_up_axis = "-1"`。

### 3. 顺带修掉一个更阴的坑（`[patch] local-keep`）

修完轴绑定去实测，面板 UI 全对，**但下发到核心是空的** —— 一查发现：

> `applyMerged(fb, mp)` 重建键位时只用「站点默认 + 服务端返回的数据」，
> **完全无视 localStorage 里已经存好的本机键位**。游客身份（或接口 401）时会走
> `applyMerged(null, null)` → 刚调好的键位被**静默重置**成站点默认。

这正是「改完键、重启又变回去」的另一个来源（上周修的两处漏洞之外的第三处）。
修法：把本机 localStorage 已存键位也算成合并链的一层；
并且管理员点「我的键位恢复默认」时先清本机副本再 sync，否则会被旧副本顶回来。

合并优先级现在是：
**出厂硬编码 → 站点 `__global__` 默认 → 站点平台默认 → 本机已存键位 → 服务端玩家键位**（后者覆盖前者）。

### 4. 真实 Chrome 复验（`_gv_axis.mjs`，**18/18 PASS**）

用的是双截龙格斗（id=18，`arcade`，FBNeo）。面板打开时的行：

```
上=左摇杆↑  下=左摇杆↓  左=左摇杆←  右=左摇杆→
A=R3  B=A  C=Y  D=B  E=LB  F=RB  Select=Select  Start=Start
```

| 检查项 | 结果 |
|---|---|
| 点「上」进入绑定态，提示文案 | ✅ 变成「按按键或拨摇杆…」 |
| 推 `axes[1] = -1`（左摇杆向上） | ✅ 槽位显示 **「左摇杆↑」** 并自动退出绑定态 |
| `__arcadeGamepad.up` | ✅ 变成字符串 `"-1"` |
| 下 / 左 / 右 依次用摇杆绑 | ✅ 左摇杆↓ / ← / → 全部成功 |
| 右摇杆也能绑 | ✅ 推 `axes[3] = -1` → 「右摇杆↑」 |
| 回归：按钮绑定没坏 | ✅ 「A」行仍能绑到按钮 11（显示 `R3`） |
| 下发到核心 | ✅ `input_player1_up_axis="-1"`、`down_axis="+1"`、`left_axis="-0"`、`right_axis="+0"`；`up_btn` **不再存在** |
| 其它键不受影响 | ✅ `b_btn=11` 保持、`analog_dpad_mode=3` 保持 |
| 点「应用并重启」 | ✅ 重启后 `up_axis` 仍是 `-1`、`b_btn` 仍是 11（**不再被重置**，这就是 local-keep 的功劳） |
| `'nul'` 不被误判 | ✅ 值为 `'nul'` 时 `_btn` / `_axis` **两个都不下发**（= 未绑定） |

面板截图：`nas-deploy/_ax_panel.png`；完整日志：`nas-deploy/_gv_axis.txt`。

### 5. 单元测试

`nas-deploy/test_key_platform.js` 新增 **J 节（21 条断言）** 专测轴绑定：
token → 轴名互转、`'nul'`/数字不误判、`_axis` 下发、不再重复下发 `_btn`、2P~4P 一致。

顺带修了这个测试**长期假失败**的老毛病：它的模块切片原来用 `lastIndexOf('})();')`，
会切到后面依赖 `document` 的 `[patch] keys-live` 块上，在 Node 里直接
`ReferenceError: document is not defined`。改为切到下一个顶层 `[patch]` 块之前。

**全量单测：363 条断言，0 失败**
（`test_key_platform` 90 / `test_port_assign` 134 / `test_turbo` 76 / `test_input_router` 33 / `test_peer_seat` 30）

### 6. 部署与影响面

- 产物 `Player-BwKb4RpM.js` md5 `25a595d14b4d2a5ce1f0f524eb83d184`（154662 B），
  **本地 / 容器内 / HTTP 分发三处 md5 一致**。
- **纯前端改动，一行数据库都没碰**；你账号（id=2）现有 3 行键位数据原样未动。
- ⚠️ **改完键要退出重进游戏才生效**（或用面板底部「应用并重启」）——
  模拟器只在 rom/核心变化时重启，改键本身不触发重启。

### 7. 你只需要做一件事

进游戏 → 按键设置 → 手柄页签 → 点「上」→ **把左摇杆往上拨一下**，
槽位应该立刻显示「左摇杆↑」。四个方向依次绑完，点底部「应用并重启」，即可开打。

---

## 十二、✅ 已修复：按键设置面板底部的按钮「乱」

按你的要求美化底部按钮区。先量了一遍**改前**的真实渲染（管理员视图），乱的原因很具体：

| 元素 | 改前实测 | 问题 |
|---|---|---|
| `.is-foot` | `display:flex; flex-wrap:nowrap; 高 182px` | 一行硬塞 4 个块，不肯换行 |
| 重置手柄 / 应用并重启 / 完成 | 43×**157** / 44×**157** / 41×**157** | 被挤成**竖排字**（「重/置/手/柄」一个字一行） |
| 三按钮样式 | radius 8 vs 10、font 12 vs 13、weight 400 vs 600 | 原生与注入两套风格混用 |
| 管理员那 4 个按钮 | 宽 148/110/110/122，占三行 | 首尾不对齐、无主次，「删除」和「保存」长得一模一样 |

改后（真实 Chrome 实测）：

| 元素 | 改后实测 |
|---|---|
| 底部整排 | 两行结构：**动作行** + **管理员区**（带分隔线），`.is-foot` 高 188px 但里面对齐 |
| 重置手柄 / 应用并重启 / 完成 | 82×34 / 95×34 / 80×34，**等高、同圆角、同字号字重**；「重置手柄」在左，另两个贴右边成组 |
| 主次关系 | 「完成」= 实心蓝（主）；「应用并重启」= 蓝色描边幽灵（次），一眼分得清 |
| 管理员区 | 独占一行：`站点默认键位` + 蓝色平台胶囊 `arcade` + 右对齐说明，下面 **2×2 等宽网格**（每个 187px） |
| 危险操作 | 「删除本站默认」= **红色**描边；「存为本站默认」= 蓝色主操作 |
| 反馈 | 点完变绿色「已保存 ✓ / 已删除 ✓ / 已恢复 ✓」，1.5 秒后自动还原 |
| 文案 | `保存为「arcade」默认` → **`存为本站默认`**（平台名进了胶囊）、`我的键位恢复默认` → **`恢复我的键位`**；每个按钮都有 `title` 说明 |

细节：按钮加了 `:hover / :active / :focus-visible` 状态（键盘 Tab 能看见聚焦环）；
底部不再横向溢出；窄屏会自然换行。

**验证（全部真实 Chrome）**
- 布局体检 `_gv_foot.mjs`：**23/23 断言**（等高、等宽、两行、网格、无溢出、样式表注入），
  管理员视图与普通用户视图**各截一张图**（`_foot_after_admin.png` / `_foot_after_user.png`）。
- 功能接线 `_gv_foot_fn.mjs`：**22/22 断言**。⚠️ 这里**故意把接口 mock 掉**才敢点 ——
  保存/删除是真写数据库的（线上需要登录），所以只验「点下去是否调到对应接口 + 反馈文案是否正确」，
  **全程零写库**。
- 端到端回归 `_gv_axis.mjs`：**18/18**（面板绑摇杆 + 下发 `_axis` + 「应用并重启」后新核心实例确实拿到）。
- 单元测试：**363 条断言 0 失败**。
- 产物 `Player-BwKb4RpM.js` **159449 B / md5 `4f89677b8137655810bc56a3d5e864ae`**，本地=容器=HTTP 三处一致。

顺带记两个坑（已写进手册）：`options.retroarchConfig` 是**开机快照**（开机后改键不回写它，
断言「有没有下发」必须重挂载后再读）；产物是 bind-mount 原地改内容、**文件名哈希不变**，
真机验证脚本必须**禁用浏览器缓存**，否则量到的是旧 JS，会得出「改了没效果」的假结论。



