# 在线游戏厅 · 方案与清单（J1900 + 飞牛 NAS + Docker）

> 目标：外网随时通过网页玩《三国战纪》《拳皇 2002》《超级玛丽》等经典游戏，手机 / 电脑 / 手柄都能玩，带账号、云存档、封面墙。

---

## 一、两个候选项目的优劣对比

| 维度 | EmulatorJS（linuxserver 镜像） | 童年游戏厅（yize8888/childhood-arcade） |
|---|---|---|
| 成熟度 | 老牌项目，社区大，文档全 | 2026-04 新项目，国人开发，飞牛社区已有人踩坑验证 |
| 界面 | 偏工具风，英文为主，不像素个"游戏厅" | 中文界面，封面墙，游戏厅氛围好 |
| 平台支持 | 25+ 平台（FC/SFC/GB/GBA/MD/PS1/街机/N64/DS…） | 街机/FC/SFC/GB/GBA/MD/PS1（对你的需求够用） |
| 账号体系 | 无，谁打开都能玩 | 有（管理员 + 用户，可登录） |
| 存档 | 只存浏览器本地 IndexedDB，**换设备丢档** | 多端云存档，换手机接档继续玩 |
| 联机 | 无 | WebRTC 开房和朋友对战（玩家 2 按键有已知 bug） |
| ROM 管理 | 后台扫描 + 按 hash 自动刮削封面/视频，很省心 | 网页上传即可，元数据较简单 |
| 部署形态 | 2 个端口（前台 + 后台），核心可自托管 | 单容器单端口，最省事 |

**结论（查证 README 后修正）**：两者底层同为 libretro + WASM，但童年游戏厅**并不基于 EmulatorJS**——它用 Nostalgist 0.21 + retroarch-emscripten v1.22.2，`data/cores/` 预置约 100MB 全部核心（完全离线、零 CDN 依赖），14 个平台（含 FBNeo + MAME2003+）已覆盖三国战纪 / 拳皇 2002 / 超级玛丽。因此融合方式是**分工互补**，不是把一个塞进另一个：

- **童年游戏厅 = 玩**：唯一日常入口，账号 + 云存档 + 联机 + 移动端虚拟按键，14 平台内全包
- **EmulatorJS = 管 + 兜底**：封面刮削车间（hash 识别 + 自动配图）+ 14 平台之外的冷门平台备胎（NDS / Saturn / 3DO / PC-FX…）

---

## 二、两个项目怎么融合（四层递进）

核心原则：**童年游戏厅是主角，EmulatorJS 是工具人**。按需逐层叠加，每层都独立可用。

### L1 · 资源层融合（零改码，阶段 2 必做）
- 两容器只读挂载同一个 `roms/` 目录，ROM 只存一份
- 童年游戏厅用自带后台上传 ROM（也可直接往目录里拷）
- EmulatorJS 后台对同一批 ROM 做"扫描 → hash 识别 → 刮封面/视频"
- 优点：`docker-compose.yml` 写好就能跑，谁都不动谁

### L2 · 数据层融合（一个小脚本，封面墙的点睛之笔）
- EmulatorJS 刮削产物（封面 PNG / 视频、识别出的游戏名）存在它的 `data/` 目录
- 写一个 10~20 行的脚本，把封面按 ROM hash 对号入座，写进童年游戏厅的 SQLite（`data/app.db` 的 `roms` 表，Drizzle ORM 结构是开源可查的）
- 效果：**游玩入口还是童年游戏厅，但每个游戏都有了官方封面**——两个项目的优点真正合体

### L3 · 入口层融合（飞牛反代，玩家只记一个域名）
```
https://game.你的域名.cn/
├── /          → 童年游戏厅 :3000（日常入口）
└── /ejs/      → EmulatorJS :8080（极客模式 / 冷门平台）
```
- 飞牛自带反代即可，无需额外容器
- **反代关键点**：童年游戏厅单端口同时跑 HTTP + WebSocket（`/api/rooms/:code/ws`，联机对战靠它），反代必须透传 `Upgrade` + `Connection` 头、超时 ≥ 60s，否则联机开房会失败
- WebRTC 默认用 Google/Cloudflare 公共 STUN；如果你家宽带是 CGNAT（无公网 IP），联机需自建 TURN——只单机玩可无视

### L4 · 内核层融合（fork 改造，最后才考虑）
- 只有一种场景值得做：想玩童年游戏厅 14 平台**之外**的游戏（如 NDS、土星、3DO）
- 做法：fork 仓库（Vue 3 + Hono，结构清晰），Player 页按平台判断——14 平台内走原 Nostalgist，之外渲染 EmulatorJS loader（`EJS_pathtodata` 指向自托管的 `/ejs/data/`）
- 不建议一上来就做：两者内核同源（都是 libretro wasm），换内核本身没有性能收益，只是多了个"能跑"

---

## 三、部署清单

### 1. 硬件与系统
- [ ] J1900 小主机（4 核），内存建议 ≥ 4GB
- [ ] 飞牛 fnOS 已启用 Docker（App 里装 Docker / Compose）
- [ ] ROM 放 SSD 分区（如 `/vol1/1000/games`），机械盘也行但封面加载略慢

### 2. Docker 镜像
| 镜像 | 用途 | 备注 |
|---|---|---|
| `yize8888/childhood-arcade:latest` | 主门户 | 默认账号 admin / admin123，**首次登录立即改密码** |
| `linuxserver/emulatorjs:latest`（拉不动就 `1.9.2`） | 封面刮削车间 + 冷门平台兜底 | 国内拉取慢 → 配镜像加速源或离线导入 |
| 飞牛自带反向代理（无需额外容器） | 统一域名 + HTTPS | 手柄 API、IndexedDB 都要求 HTTPS 安全上下文 |

### 3. 端口规划（仅内网使用，不映射公网）
| 端口 | 服务 |
|---|---|
| 3000 | 童年游戏厅（主入口） |
| 8080 | EmulatorJS 游玩前台 |
| 4300 | EmulatorJS 管理后台（扫 ROM、刮封面、下载核心） |
| 443 | 路由器 → 飞牛反代 → 主入口 |

### 4. 目录结构
```
games/
├── docker-compose.yml
├── arcade-data/        # 童年游戏厅数据（账号、云存档、上传的 ROM）
├── ejs-config/         # EmulatorJS 配置
└── roms/               # 共享 ROM 库（两容器都挂这里）
    ├── fbneo/          # 三国战纪、拳皇2002、恐龙快打（街机）
    │   └── neogeo.zip  # NeoGeo BIOS（拳皇必需）
    ├── nes/            # 超级玛丽、魂斗罗、坦克大战
    ├── sfc/  gb/  gba/  md/  psx/
```

### 5. 你点名游戏的内核对照表
| 游戏 | 平台 | 推荐核心 | 注意事项 |
|---|---|---|---|
| 三国战纪（IGS PGM） | 街机 | FBNeo | ROM 版本要和 FBNeo 核心的 romset 版本匹配，建议直接找"FBNeo 合集"打包，避免单 rom 报错 |
| 拳皇 2002（NeoGeo） | 街机 | FBNeo | **必须**同时放 `neogeo.zip` BIOS，版本同样要配套 |
| 超级玛丽 | FC/NES | fceumm | 兼容性极好，随便跑 |
| 建议顺带：恐龙快打、名将、魂斗罗、合金弹头 | 街机/FC | FBNeo / fceumm | 同上规则 |

### 6. 外网接入与联机方案（三选一，可组合）

**先懂原理再选型**：童年游戏厅的联机 = 房主浏览器跑游戏 → `captureStream` 视频流 → WebRTC 点对点推给朋友。**游戏画面不经过服务器**，NAS 只负责网页 + 信令 WebSocket。所以外网方案只需满足三件事：HTTPS（手柄/存档的前提）、WebSocket 透传（开房）、STUN 可达（打洞）。

| 方案 | 延迟 | 要公网 IP？ | 朋友门槛 | 联机保障 | 定位 |
|---|---|---|---|---|---|
| **A. DDNS + 443 直连** | 最低 | 要（IPv4 或 IPv6） | 零，发链接即玩 | 纯 P2P；CGNAT 才需 TURN | 有公网 IP 时的首选主力 |
| **B. 组网工具（节点小宝 ✅ 已在用 / Tailscale）** | 极低（P2P 直连） | 不要 | 每人装客户端入网 | 全端口互通，STUN/TURN 小宝自带（国内直连率 95%+） | 自己 + 家人 + 固定小圈子 |
| **C. Cloudflare Tunnel** | 页面略增延迟，联机仍是 P2P | 不要（专治没公网 IP） | 零，发链接即玩 | 靠公共 STUN；打洞失败需 TURN | 朋友多、即开即玩 |

- **方案 A**：光猫桥接 + 路由器 DDNS，只映射 443 → 飞牛反代。家里宽带有 IPv6 的话（现在很普遍），直连成功率很高。
- **方案 B（你的现状）**：节点小宝已原生集成在飞牛应用中心，组网后通过 `虚拟IP:3000` 直访，全端口互通（SMB / 管理后台 / 游戏厅都能用），小宝自带 STUN+TURN 双栈，联机打洞不需要你操心。两个注意点：① 组网内是 `http://100.66.x.x` 没有 HTTPS，而**浏览器手柄 API 要求安全上下文**——组网内玩用虚拟按键/键盘没影响，想插手柄就走 CF 域名；② 给朋友用仍需装客户端，门槛高于 CF 链接。
- **方案 C**：NAS 跑 `cloudflared` 容器（compose 里已备好注释段），CF 控制台把域名指到 `http://childhood-arcade:3000`，WebSocket 默认放行。**注意 CF 免费版有 100MB 请求体限制 → ROM 一律在内网上传，外网只负责玩。**
- **Vercel 排除**：它是静态/Serverless 托管平台，做不了到 NAS 的 WebSocket 长连接反代，跟联机需求根本不匹配，顶多挂个宣传页，别在这上面花时间。
- **CGNAT + 联机兜底**：TURN 必须部署在**有公网 IP 的机器**上（便宜 VPS 装 coturn，或用 Cloudflare Calls TURN 服务），放在 NAS 上毫无意义——NAS 自己都没公网 IP。
- **推荐组合**：B 给自己回家管理用，C（或 A）给朋友玩，两者互不冲突，一起上。
- **延迟预期管理**：视频流联机天然多 100~200ms，街机过关、FTG 对战能玩但没有本地爽；同屏双人（一个手柄分两口）体验永远最好。
- **通用底线**：无论选哪个方案，3000/8080/4300 一律不直接暴露公网；EmulatorJS 管理后台只在内网用。浏览器要求 Chrome / Edge / Safari（Firefox 手柄支持不全）。

### 7. 安全与备份
- [ ] 立刻改掉 admin/admin123；如不需要朋友注册，关闭开放注册
- [ ] `MAX_UPLOAD_BYTES` 默认 100MB，街机大 ROM 集合可按需调大
- [ ] 定期备份 `arcade-data/`（云存档都在这里面）——飞牛计划任务每日同步即可

---

## 四、分阶段实施路线

**阶段 1 · 半天跑通（先爽起来）**
只部署童年游戏厅单容器 → 传 3 个游戏（三国战纪 / 2002 / 超级玛丽）→ 内网验证手机、电脑、手柄都能玩。

**阶段 2 · 叠加 EmulatorJS（工具人上线）**
再起第二个容器，共享 ROM 目录 → EmulatorJS 后台扫描刮封面（L1）→ 跑脚本把封面导入童年游戏厅（L2）→ 飞牛反代统一入口（L3）。注意：童年游戏厅核心已全本地化，**不需要**也不应该去动它的内核。

**阶段 3 · 精修（可选）**
- 飞牛反代 + DDNS + HTTPS，域名回家
- 定制统一门户首页（换皮成你自己的"游戏厅招牌"）
- 存档自动备份计划任务；童年游戏厅联机对战待作者修复玩家 2 bug 后再开

---

## 五、已知坑（提前避开）

1. **镜像拉取失败**：国内网络老问题，配加速源或用飞牛"离线镜像导入"。
2. **FBNeo romset 版本不匹配**：街机游戏报"missing files"基本都是这个原因，rom 版本要跟核心版本配套。
3. **不用 HTTPS 时**：部分浏览器下手柄、全屏、存档功能会异常，务必上 HTTPS。
4. **童年游戏厅较新**：联机对战玩家 2 部分按键失效是已知问题（单机不受影响），遇到可到 GitHub 提 issue。
5. **反代忘了透传 WebSocket**：联机开房失败的第一嫌疑。透传 `Upgrade`/`Connection` 头、超时 ≥ 60s。
6. **PSX BIOS 自备**：街机 / FDS BIOS 已预置，但 PS1 的 BIOS 要自己放 `data/bios/psx/`。
7. **J1900 定位**：它不参与模拟，所以 8/16 位机、街机都没压力；但别指望在 2010 年前的手机浏览器上流畅跑 PS1——模拟压力在访客设备端。

---

## 六、docker-compose.yml（初稿，随方案文档同目录提供）

见同目录 `docker-compose.yml`，先只起童年游戏厅，EmulatorJS 段已注释好，阶段 2 时取消注释即可。

---

## 七、Cloudflare Tunnel 详细实施（给朋友玩的主力方案）

### 前置条件
- ✅ 域名已就绪：`arcade.example.com`（腾讯云注册）已托管到 CF，将使用子域 `arcade.example.com`
- NAS 能拉取 `cloudflare/cloudflared` 镜像
- 腾讯云注册域名两个注意点：① 只改 DNS 托管不动注册商，域名仍留在腾讯云管理，续费不受影响；② 国内建站需要 ICP 备案，但你走的是 CF 海外边缘节点，**不需要备案**，CF 会自动为 `arcade.example.com` 签发免费证书

### 快速验证（10 分钟，先测联机通不通，再决定正式部署）
```bash
docker run --rm cloudflare/cloudflared:latest tunnel --url http://<NAS内网IP>:3000
```
命令会输出一个 `https://xxx.trycloudflare.com` 临时网址 → 手机开流量访问 → 能进游戏厅、能开房、第二台设备能加入，就说明方案成立。临时地址随机且会过期，仅供测试。

### 正式部署四步
1. CF 控制台 → Zero Trust → Networks → Tunnels → Create a tunnel → 复制 Token
2. `docker-compose.yml` 取消 cloudflared 注释段，填入 `TUNNEL_TOKEN`，`up -d`
3. 回到 CF 控制台配 Public Hostname：子域填 `game`、域选 `arcade.example.com` → Service 选 `HTTP` → 填 `childhood-arcade:3000`（与游戏厅同 compose 网络可直接用服务名；单独 docker run 则填 `NAS内网IP:3000`）。保存后 CF 会自动在 DNS 里创建 `arcade.example.com` 的 CNAME 记录，指向隧道
4. 浏览器访问 `https://arcade.example.com` 全流程验证：登录 → 玩单机 → 开房 → 朋友加入

### 这套架构为什么安全
cloudflared 从 NAS **向外**拨号到 CF 边缘，NAS 不开任何入站端口，3000/8080/4300 外网根本摸不到；CF 还顺带挡掉 DDoS 和漏洞扫描。

### 必知的五个关键点
1. **WebSocket**：CF 默认放行，联机信令 `/api/rooms/*/ws` 就靠它；万一开房失败，先检查域名的 WebSocket 开关和隧道健康状态
2. **100 秒空闲超时**：CF 会掐断长时间无流量的连接，开房挂机太久掉线属正常现象，重开即可
3. **请求体 100MB 上限（免费版）**：ROM 一律在内网上传，外网只负责玩
4. **缓存规则**：给 `/api/*` 设 Bypass Cache，ROM 等静态文件可缓存加速二次加载
5. **国内速度**：免费版流量走 CF 海外边缘节点，页面和核心文件首次加载偏慢（核心只需加载一次，之后浏览器缓存）；联机画面走 P2P，**不经过 CF，不受影响**；介意可研究社区里的"优选 IP"方案

### TURN 兜底（CGNAT 朋友联机失败时才需要）
- Cloudflare Calls TURN 服务有免费额度，是现成的 TURN
- 前提是童年游戏厅支持配置 TURN——README 只说"CGNAT 需自建"，先翻管理后台的站点设置找 TURN 配置项；没有就给作者提 issue，或 fork 后自己加（代码是 Vue3+Hono，改动很小）
- 走节点小宝组网的朋友不受此限制，打洞由小宝的 STUN+TURN 栈负责

### 与节点小宝的最终分工
| 谁 | 走哪条路 | 干什么 |
|---|---|---|
| 自己 / 家人 | 节点小宝虚拟 IP | 全端口直访：游戏厅、SMB、管理后台 |
| 朋友 | CF 域名链接 | 发链接即玩，插手柄（HTTPS 齐全） |
| 运维 | 节点小宝 | 回家改配置、传 ROM、刮封面 |
